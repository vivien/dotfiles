#!/bin/bash

REPO=~/.dotfiles

pushd $REPO > /dev/null
case $1 in
    pull)
        git pull origin master
        ;;
    push)
        git add .
        git commit -m "$2"
        git push origin master
        ;;
    *)
        git status
        ;;
esac
popd > /dev/null

exit
