#!/bin/bash

REPO=~/.dotfiles

pushd $REPO > /dev/null
case $1 in
    pull)
        git pull origin master
        ;;
    push)
        git add .
        git commit -am "$2"
        git push origin master
        ;;
    *)
        git status --short
        ;;
esac
popd > /dev/null

exit
